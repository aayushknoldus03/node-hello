pipeline {
    agent any
    stages {
        stage('installing dependency') {
            steps {
                sh 'npm install'
                }
        }
        stage('Start server') {
            steps {
                sh 'npm start'
            }
        }
        }
    }
